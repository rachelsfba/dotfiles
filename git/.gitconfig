[user]
    email = rshaw@beijaflor.es
    name = Rachel Shaw
[push]
    default = simple
[credential]
    helper = cache --timeout 3600
[core]
    editor = vim
[filter "spabs"]
    clean = expand --initial -t 4
    smudge = expand --initial -t 4
    required
[merge]
    renormalize = true
